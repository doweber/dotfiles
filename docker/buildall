#!/bin/bash

platform='unknown'
unamestr=`uname`
if [[ "$unamestr" == 'Linux' ]]; then
  platform='linux'

  # build the devuser image
  GROUPID=`id -g` USERID=`id -u` bash -c 'cat devuser/Dockerfile | envsubst | sudo docker build -t foobar/devuser:base -'
  sudo docker build -t foobar/ruby:dev rubydev/

elif [[ "$unamestr" == 'Darwin' ]]; then
  platform='Darwin'

  # build the devuser image
  GROUPID=`id -g` USERID=`id -u` bash -c 'cat devuser/Dockerfile | envsubst | docker build -t foobar/devuser:base -'
  docker build -t foobar/ruby:dev rubydev/

fi

